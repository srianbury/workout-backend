import { expect } from "chai";

describe("sample", () => {
  describe("sample", () => {
    it("runs the test suite", async () => {
      // Sample to ensure the tests actually run
      expect(true).to.eql(true);
    });
  });
});
